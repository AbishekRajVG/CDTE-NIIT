$(function() {

	var products = [

	[ '1', 'ABC' ], [ '2', 'FED' ], [ '3', 'SDF' ], [ '4', 'AIS' ],
			[ '5', 'POE' ], [ '6', 'SQE' ], [ '7', 'WGT' ], [ '8', 'QKU' ]

	];

	var $table = $('#productListTable');

	// code when only on all products
	if ($table.lenght) {

		console.log('Inside the Table!');

	}

});